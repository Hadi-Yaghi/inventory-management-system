[build]
builder = "RAILPACK"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar target/code-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

