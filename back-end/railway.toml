[build]
builder = "RAILPACK"

[deploy]
startCommand = "java -XX:+UseG1GC -XX:MaxRAMPercentage=75.0 -jar target/app.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
